# Next.js web app — public client config. Copy to .env.local and fill in.
# These are PUBLIC (anon) values, safe to expose to the browser. No service role key here.
NEXT_PUBLIC_SUPABASE_URL=https://<project-ref>.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJ...
